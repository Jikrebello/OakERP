# Findings

- The root `README.md` still describes a fictional top-level `/Apps`, `/Core`, `/Infrastructure` layout and points to a root `README-dev.md` that does not exist.
- `OakERP.Docs/README-dev.md` is still a stub hub instead of a real onboarding entrypoint.
- `db-setup.md` still shows `/OakERP.Solution` path language and an outdated `5432:5432` Docker mapping.
- `oakERP-testing-guide.md` contains stale framework and workflow assumptions that no longer match the current harness.
- `docs/architecture/project-map.md` is largely current, but it can better reflect current API Swagger/runtime ownership and the shared MAUI host-core split.
